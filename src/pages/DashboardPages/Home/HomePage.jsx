import React from "react";

const HomePage = () => {
  return (
    <div
      style={{
        padding: "40px",
        fontFamily:
          "system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif",
      }}
    >
      <h1 style={{ marginBottom: "12px" }}>Dashboard</h1>
      <p style={{ margin: 0 }}>You are successfully logged in.</p>
    </div>
  );
};

export default HomePage;
